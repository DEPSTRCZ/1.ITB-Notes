lsi: false
safe: true
source: ["./ZÁPISKY"]
remote_theme: just-the-docs/just-the-docs
incremental: true
domain: zapisky.depstr.eu       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://zapisky.depstr.eu
timezone: Europe/Prague
highlighter: rouge
color_scheme: dark
show_dir_listing: true
layout: default
nav_order: 4
gist:
  noscript: false
kramdown:
  math_engine: mathjax
  syntax_highlighter: rouge

plugins: ["jekyll-offline"]

defaults:
  scope:
    path: ""
    type: "pages"
  values:
    permalink: "/:path/:basename/"
